<Window x:Class="ShopBanHang_OfflineFirst.Views.ServerConfigDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Cấu hình kết nối server" Height="320" Width="520"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock TextWrapping="Wrap" Margin="0,0,0,12"
                   Text="URL API trực tiếp (ngrok/LAN). Ưu tiên hơn appsettings.json. Có thể sửa khi ngrok đổi địa chỉ."/>
        <TextBox x:Name="txtServerUrl" Grid.Row="1" Height="28" Margin="0,0,0,16"/>

        <TextBlock Grid.Row="2" TextWrapping="Wrap" Margin="0,0,0,8"
                   Text="URL discovery cố định (tùy chọn): client GET file JSON để lấy apiBaseUrl mới khi ngrok đổi."/>
        <TextBox x:Name="txtDiscoveryUrl" Grid.Row="3" Height="28"/>

        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="Hủy" Width="90" Margin="0,0,8,0" IsCancel="True"/>
            <Button Content="Lưu" Width="90" IsDefault="True" Click="BtnSave_Click"/>
        </StackPanel>
    </Grid>
</Window>
